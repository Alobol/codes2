#include <iostream>

int main() {
    int a, b, c;

    std::cout << "Введите три целых числа: ";
    std::cin >> a >> b >> c;

    int max = (a > b) ? (a > c ? a : c) : (b > c ? b : c);

    std::cout << "Максимальное число: " << max << std::endl;

    return 0;
}
