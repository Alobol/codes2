#include <iostream>
#include <string>

string checkEvenOrOdd(int number) {
    return (number % 2 == 0) ? "Четное" : "Нечетное";
}

int main() {
    int num;

    cout << "Введите целое число: ";
    cin >> num;

    cout << "Число " << num << " является: " << checkEvenOrOdd(num) << std::endl;

    return 0;
}
