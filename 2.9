#include <iostream>
#include <string>

int main() {
    std::string str;
    std::cout << "Введите строку: ";
    std::getline(std::cin, str);
    
    std::cout << (str.find(' ') != std::string::npos ? "с пробелами" : "без пробелов") << std::endl;
    
    return 0;
}
