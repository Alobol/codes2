#include <iostream>
usingnamespase
cout << "Введите число: ";
cin >> b;
cout << "Факториал числа: "<<fact(b);
int fact(int f)
{
if (f < 0)
return 0;
if (f == 0)
return 1;
else
 {
return f * fact(f - 1);
 }
}